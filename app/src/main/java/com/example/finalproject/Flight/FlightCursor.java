/*
package com.example.finalproject.Flight;

import android.database.Cursor;
import android.database.CursorWrapper;

public class FlightCursor extends CursorWrapper {
    */
/**
     * Creates a cursor wrapper.
     *
     * @param cursor The underlying cursor to wrap.
     *//*

    public FlightCursor(Cursor cursor) {
        super(cursor);
    }

    public FlightEntity getFlight(){
        Long id = Long.fromString(getString(getColumnIndex(FlightDatabaseSchema.FlightTable.Cols.ID)));
        String location = getString(getColumnIndex(FlightDatabaseSchema.FlightTable.Cols.Location));

    }
}
*/
